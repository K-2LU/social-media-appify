export const getComments = (req, res) => {
  // Minimal stub: return empty comments list
  return res.status(200).json({ comments: [] })
}

// Add more comment-related controller functions here later
